
<h1>Views</h1>

<?php
if(is_array($this->views)){

    $table = $this->html->table()->class('table');

    foreach($this->views as $view){

        $row = $this->html->row(array(
            $view['name'],
            $view['file']
        ));

    }

    echo $table;

}
?>
